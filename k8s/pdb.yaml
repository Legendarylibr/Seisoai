apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: seisoai-app-pdb
  namespace: seisoai
  labels:
    app: seisoai
    component: backend
spec:
  # Ensure at least 2 pods are always available
  minAvailable: 2
  selector:
    matchLabels:
      app: seisoai
      component: backend

