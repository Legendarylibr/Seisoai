providers = ["node"]

[phases.setup]
nixPkgs = ['nodejs', 'npm']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node start-app.js'

[variables]
NODE_ENV = "production"