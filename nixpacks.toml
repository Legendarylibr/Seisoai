providers = ["node"]

[phases.setup]
nixPkgs = ['nodejs']

[phases.install]
cmds = ['npm install', 'cd backend && npm install']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npx tsx start-app.ts'

[variables]
NODE_ENV = "production"