providers = ["node"]

[phases.setup]
nixPkgs = ['nodejs', 'ffmpeg']

[phases.install]
cmds = ['npm install', 'cd backend && npm install']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npx tsx serve-real-backend.ts'

[variables]
NODE_ENV = "production"